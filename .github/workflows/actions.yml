name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Python Pytest Github Action
      uses: fylein/python-pytest-github-action@v2
      with:
        args: pytest --log-level=INFO
